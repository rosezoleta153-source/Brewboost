<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>BrewBoost — Cozy Café (Accessible)</title>
  <meta name="description" content="BrewBoost — pre-order coffee prototype (cozy café vibe). Accessible product modals and offline-friendly fallbacks." />
  <style>
    :root{
      --bg:#0b0b08;
      --card:#1b1612;
      --muted:#b9aaa0;
      --text:#f6efe9;
      --accent:#b9772a; /* warm gold */
      --accent-2:#e6b86a;
      --glass: rgba(255,255,255,0.025);
      --radius:16px;
      --maxw:1100px;
      --shadow: 0 12px 28px rgba(0,0,0,0.6);
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0;
      font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, Arial;
      background:
        radial-gradient(800px 300px at 10% 10%, rgba(230,184,106,0.03), transparent),
        linear-gradient(180deg,var(--bg),#060505 140%);
      color:var(--text);
      -webkit-font-smoothing:antialiased;
      padding:28px 18px;
      display:flex;
      justify-content:center;
    }
    .app{
      width:100%;
      max-width:var(--maxw);
      background: linear-gradient(180deg, rgba(255,255,255,0.01), rgba(255,255,255,0.00));
      border-radius:18px;
      padding:20px;
      box-shadow:var(--shadow);
      border:1px solid rgba(255,255,255,0.02);
      overflow:hidden;
    }
    header{display:flex;align-items:center;justify-content:space-between;gap:12px}
    .brand{display:flex;gap:12px;align-items:center}
    .logo{
      width:56px;height:56px;border-radius:12px;
      background:linear-gradient(135deg,var(--accent),var(--accent-2));
      display:flex;align-items:center;justify-content:center;color:#2a1406;font-weight:800;font-size:20px;
      box-shadow:0 8px 22px rgba(185,138,72,0.12);
    }
    .brand h1{margin:0;font-size:18px}
    .brand p{margin:0;font-size:12px;color:var(--muted)}
    nav{display:flex;gap:8px;align-items:center}
    nav a{color:var(--muted);text-decoration:none;padding:8px 12px;border-radius:10px}
    nav a:hover, nav a.active{background:var(--glass);color:var(--text)}
    .main{display:grid;grid-template-columns:1fr 360px;gap:20px;margin-top:18px}
    .left{display:flex;flex-direction:column;gap:18px}

    /* HERO */
    .hero{
      position:relative;border-radius:14px;overflow:hidden;min-height:220px;
      display:flex;align-items:center;background:linear-gradient(180deg, rgba(0,0,0,0.18), rgba(0,0,0,0.22));
      border:1px solid rgba(255,255,255,0.02);
    }
    .hero .bg{position:absolute;inset:0;background-size:cover;background-position:center;filter:blur(6px) brightness(.45);transform:scale(1.04)}
    .hero .overlay{position:relative;padding:28px;z-index:2;display:flex;gap:12px;align-items:center}
    .hero h2{margin:0;font-size:26px;line-height:1.05}
    .hero p{color:var(--muted);margin-top:8px}
    .cta{margin-top:12px;display:flex;gap:10px;align-items:center}
    .btn{padding:10px 14px;border-radius:12px;border:none;cursor:pointer;font-weight:700}
    .btn-primary{background:linear-gradient(90deg,var(--accent),var(--accent-2));color:#2b1608}
    .btn-ghost{background:transparent;border:1px solid rgba(255,255,255,0.04);color:var(--text)}

    /* content cards */
    .card{background:linear-gradient(180deg, rgba(255,255,255,0.01), rgba(255,255,255,0.00));padding:14px;border-radius:12px;border:1px solid rgba(255,255,255,0.02)}
    h3{margin:0;color:var(--accent)}
    .chips{display:flex;gap:10px;flex-wrap:wrap;margin-top:10px}
    .chip{background:rgba(255,255,255,0.02);padding:6px 10px;border-radius:999px;font-size:13px;color:var(--muted);border:1px solid rgba(255,255,255,0.02)}

    /* products grid */
    .products{display:grid;grid-template-columns:repeat(2,1fr);gap:14px;margin-top:12px}
    .product{
      display:flex;gap:12px;align-items:center;padding:12px;border-radius:12px;cursor:pointer;
      background:linear-gradient(180deg, rgba(255,255,255,0.01), rgba(255,255,255,0.00));
      border:1px solid rgba(255,255,255,0.02);
      transition:transform .18s ease, box-shadow .18s ease;
    }
    .product:focus, .product:hover{transform:translateY(-6px);box-shadow:0 18px 36px rgba(0,0,0,0.6);outline:none}
    .thumb{width:86px;height:86px;border-radius:10px;overflow:hidden;flex-shrink:0;background:#111;display:block}
    .thumb img{width:100%;height:100%;object-fit:cover;display:block}
    .meta{flex:1}
    .meta h4{margin:0 0 6px 0}
    .meta p{margin:0;color:var(--muted);font-size:13px}
    .price{font-weight:800;color:var(--accent-2);margin-left:10px}

    /* sidebar */
    .sidebar{display:flex;flex-direction:column;gap:12px;position:relative}
    .cart .line{display:flex;gap:8px;align-items:center;margin-bottom:10px}
    .total{font-weight:800;color:var(--accent-2)}

    /* modal */
    .modal-backdrop{position:fixed;inset:0;background:rgba(0,0,0,0.6);display:none;align-items:center;justify-content:center;z-index:999}
    .modal{width:min(760px,92%);background:linear-gradient(180deg, #1d1410, #14100d);border-radius:14px;padding:16px;border:1px solid rgba(255,255,255,0.03);display:grid;grid-template-columns:1fr 320px;gap:12px}
    .modal .m-thumb{width:100%;height:320px;border-radius:10px;overflow:hidden;background:#111}
    .modal .m-thumb img{width:100%;height:100%;object-fit:cover;display:block}
    .modal h4{margin:0 0 8px 0}
    .modal p{color:var(--muted)}
    .modal .m-actions{display:flex;gap:8px;align-items:center;justify-content:flex-end}
    .visually-hidden{position:absolute!important;height:1px;width:1px;overflow:hidden;clip:rect(1px,1px,1px,1px);white-space:nowrap;border:0;padding:0;margin:-1px}

    footer{margin-top:18px;padding-top:12px;border-top:1px solid rgba(255,255,255,0.02);display:flex;justify-content:space-between;align-items:center;font-size:13px;color:var(--muted)}

    @media (max-width:980px){.main{grid-template-columns:1fr}.products{grid-template-columns:1fr}.modal{grid-template-columns:1fr;}.modal .m-thumb{height:220px}}
  </style>
</head>
<body>
  <div class="app" id="app">
    <header>
      <div class="brand">
        <div class="logo" aria-hidden="true">BB</div>
        <div>
          <h1>BrewBoost</h1>
          <p>Cozy café • pre-order demo</p>
        </div>
      </div>
      <nav aria-label="Main navigation">
        <a href="#home" class="navlink active" data-target="home">Home</a>
        <a href="#about" class="navlink" data-target="about">About</a>
        <a href="#products" class="navlink" data-target="products">Products</a>
        <a href="#contact" class="navlink" data-target="contact">Contact</a>
      </nav>
    </header>

    <main class="main" id="main">
      <div class="left">
        <!-- HERO -->
        <section id="home" class="hero card" aria-labelledby="homeTitle">
          <div class="bg" id="hero-bg" aria-hidden="true"></div>
          <div class="overlay">
            <div style="flex:1">
              <h2 id="homeTitle">BrewBoost — skip the line. savor the coffee.</h2>
              <p>Order ahead, customize your cup, and pick it up warm. Designed for research & demo.</p>
              <div class="cta">
                <button class="btn btn-primary" id="quick-order">Order Now</button>
                <button class="btn btn-ghost" id="howit">How it works</button>
              </div>
              <div class="chips" style="margin-top:12px">
                <div class="chip"><strong>Open (mock)</strong><div style="font-size:12px;color:var(--muted)">7:00 — 18:00</div></div>
                <div class="chip"><strong>Pickup ETA</strong><div id="eta" style="font-size:12px;color:var(--muted)">~10 minutes</div></div>
                <div class="chip"><strong>Use</strong><div style="font-size:12px;color:var(--muted)">Collect feedback</div></div>
              </div>
            </div>
            <div style="width:160px;text-align:right" aria-hidden="true">
              <!-- decorative cup svg as cozy accent -->
              <svg width="120" height="120" viewBox="0 0 64 64" fill="none" xmlns="http://www.w3.org/2000/svg">
                <rect x="6" y="20" width="40" height="28" rx="5" fill="#1b1612" stroke="rgba(185,138,72,0.06)"/>
                <path d="M46 26c3 0 6 2 6 7s-3 7-6 7" stroke="#3b3b3b" stroke-width="2" fill="none"/>
              </svg>
            </div>
          </div>
        </section>

        <!-- ABOUT -->
        <section id="about" class="card" aria-labelledby="aboutTitle">
          <h3 id="aboutTitle">About BrewBoost</h3>
          <p style="color:var(--muted)">A minimal pre-order concept for academic research. Use this prototype to test pickup behavior, preferences, and conversion under a cozy café brand aesthetic.</p>
          <div class="chips">
            <div class="chip">Target: Students & Professionals</div>
            <div class="chip">Metrics: conversion, AOV</div>
            <div class="chip">Prototype: offline-friendly</div>
          </div>
        </section>

        <!-- PRODUCTS -->
        <section id="products" class="card" aria-labelledby="productsTitle">
          <h3 id="productsTitle">Menu — Featured</h3>
          <div class="products" id="products-list" role="list">
            <!-- JS will render items -->
          </div>
        </section>

        <!-- CONTACT -->
        <section id="contact" class="card" aria-labelledby="contactTitle">
          <h3 id="contactTitle">Contact / Feedback</h3>
          <form id="contactForm">
            <label class="visually-hidden" for="name">Your name</label>
            <input id="name" type="text" placeholder="Your name" required />
            <label class="visually-hidden" for="email">Your email</label>
            <input id="email" type="email" placeholder="Email" required />
            <label class="visually-hidden" for="purpose">Purpose</label>
            <select id="purpose">
              <option value="feedback">Feedback</option>
              <option value="research">Participate in research</option>
              <option value="partnership">Partnerships</option>
            </select>
            <label class="visually-hidden" for="message">Message</label>
            <textarea id="message" placeholder="Message" required></textarea>
            <div style="display:flex;gap:10px;align-items:center">
              <button class="btn btn-primary" type="submit">Send</button>
              <button type="button" class="btn btn-ghost" id="clear">Clear</button>
            </div>
            <p id="formMsg" style="color:var(--accent-2);margin-top:8px"></p>
          </form>
        </section>
      </div>

      <!-- SIDEBAR -->
      <aside class="sidebar" aria-label="Sidebar">
        <div class="card cart" aria-live="polite">
          <h4 style="margin:0 0 10px 0;color:var(--accent)">Order Preview</h4>
          <div id="cartLines"><div class="line">No items yet</div></div>
          <div style="display:flex;justify-content:space-between;align-items:center;margin-top:12px">
            <div class="muted">Total</div>
            <div class="total" id="cartTotal">₱0.00</div>
          </div>
          <div style="margin-top:12px;display:flex;gap:8px">
            <button class="btn btn-primary" id="checkout">Checkout (mock)</button>
            <button class="btn btn-ghost" id="empty">Empty</button>
          </div>
        </div>

        <div class="card" aria-hidden="false">
          <h4 style="margin:0 0 8px 0;color:var(--accent)">Controls</h4>
          <div style="display:flex;gap:8px;flex-wrap:wrap">
            <div class="chip">Cozy • Warm</div>
            <div class="chip" id="imgRefreshBtn" tabindex="0" role="button" aria-pressed="false">Refresh Images</div>
          </div>
          <p style="color:var(--muted);font-size:13px;margin-top:10px">Tip: Refresh images to load new Unsplash photos. Images load from Unsplash when online; fallback SVG shows offline.</p>
        </div>

        <div class="card" style="text-align:center;color:var(--muted)">
          <small>© 2025 BrewBoost — academic prototype</small>
        </div>
      </aside>
    </main>

    <footer>
      <div>Built for Technopreneurship project</div>
      <div style="color:var(--muted)">Cozy design • Accessible • Easy to present</div>
    </footer>
  </div>

  <!-- Accessible modal -->
  <div class="modal-backdrop" id="modalBackdrop" role="presentation" aria-hidden="true">
    <div class="modal" role="dialog" aria-modal="true" aria-labelledby="modalTitle" aria-describedby="modalDesc" id="modal">
      <div>
        <div class="m-thumb" id="modalImageWrap">
          <!-- image inserted by JS -->
        </div>
      </div>
      <div style="display:flex;flex-direction:column;gap:10px">
        <div>
          <h4 id="modalTitle">Product</h4>
          <p id="modalDesc">Description</p>
          <div style="margin-top:8px;"><strong id="modalPrice">₱0</strong></div>
        </div>
        <div style="margin-top:auto;display:flex;gap:8px;align-items:center;justify-content:flex-end">
          <button class="btn btn-ghost" id="modalClose">Close</button>
          <button class="btn btn-primary" id="modalAdd">Add to cart</button>
        </div>
      </div>
    </div>
  </div>

  <script>
    /* ---------------- CONFIG ----------------
       Use Unsplash source for images (online). Fallback: inline SVG if image fails.
    ------------------------------------------*/
    const products = [
      { id: 'espresso', name: 'Espresso Boost', desc: 'Rich, bold shot. Intense aroma.', price: 70, query: 'espresso coffee' },
      { id: 'caramel', name: 'Caramel Cloud', desc: 'Sweet caramel, cream, delicate foam.', price: 120, query: 'caramel latte' },
      { id: 'mocha', name: 'Mocha Fusion', desc: 'Chocolate and coffee, velvety.', price: 140, query: 'mocha coffee' },
      { id: 'coldbrew', name: 'Cold Brew Chill', desc: 'Smooth cold brew, chilled perfection.', price: 130, query: 'cold brew coffee' }
    ];

    function unsplashFor(query, w=600, h=420){
      return `https://source.unsplash.com/${w}x${h}/?${encodeURIComponent(query)},coffee`;
    }

    // fallback SVG image (data URL)
    const FALLBACK_SVG = 'data:image/svg+xml;utf8,' + encodeURIComponent(`
      <svg xmlns="http://www.w3.org/2000/svg" width="800" height="600" viewBox="0 0 800 600">
        <rect width="100%" height="100%" fill="#1b1512"/>
        <g fill="#cfa775">
          <ellipse cx="200" cy="420" rx="150" ry="30" fill="#0e0b0a" opacity="0.2"/>
          <path d="M520 160c-60-20-140-20-200 0-10 3-18 9-22 18-6 12-3 26 6 36 18 20 46 30 72 30 40 0 74-22 94-52 8-12 8-28-2-32z" fill="#e6b86a"/>
          <rect x="80" y="220" width="260" height="180" rx="20" fill="#2b2019"/>
          <path d="M120 240h200v120a30 30 0 0 1-30 30H150a30 30 0 0 1-30-30V240z" fill="#3a2a21"/>
          <circle cx="480" cy="330" r="80" fill="#2b2019"/>
          <text x="120" y="420" fill="#bfa076" font-size="28" font-family="Arial">BrewBoost</text>
        </g>
      </svg>`);

    // set hero background
    document.getElementById('hero-bg').style.backgroundImage = `url('${unsplashFor('coffee shop interior', 1400,700)}')`;

    // render products with accessible interactions
    const productsList = document.getElementById('products-list');
    function renderProducts(){
      productsList.innerHTML = '';
      products.forEach(p=>{
        const item = document.createElement('div');
        item.className = 'product';
        item.setAttribute('role','listitem');
        item.setAttribute('tabindex','0');
        item.setAttribute('aria-label', `${p.name}, price ₱${p.price}. Press Enter to view details.`);
        item.innerHTML = `
          <div class="thumb" aria-hidden="true">
            <img loading="lazy" src="${unsplashFor(p.query,400,320)}" alt="${p.name}">
          </div>
          <div class="meta">
            <h4>${p.name} <span style="color:var(--muted);font-weight:600">• ₱${p.price}</span></h4>
            <p>${p.desc}</p>
          </div>
        `;
        // keyboard + click open modal
        item.addEventListener('click', ()=> openModal(p));
        item.addEventListener('keydown', (e)=> { if(e.key === 'Enter') openModal(p); });
        // image fallback handler
        const img = item.querySelector('img');
        img.addEventListener('error', ()=> img.src = FALLBACK_SVG);
        productsList.appendChild(item);
      });
    }
    renderProducts();

    /* ---------- Cart logic (simple) ---------- */
    const cart = {};
    function addToCart(id, qty=1){
      cart[id] = (cart[id]||0) + qty;
      renderCart();
    }
    function removeOne(id){
      if(!cart[id]) return;
      cart[id]--;
      if(cart[id] <= 0) delete cart[id];
      renderCart();
    }
    function emptyCart(){
      for(let k in cart) delete cart[k];
      renderCart();
    }
    function renderCart(){
      const lines = document.getElementById('cartLines');
      lines.innerHTML = '';
      let total = 0;
      const keys = Object.keys(cart);
      if(keys.length === 0){
        lines.innerHTML = '<div class="line">No items yet</div>';
      } else {
        keys.forEach(id=>{
          const p = products.find(x=>x.id===id);
          const qty = cart[id];
          const div = document.createElement('div');
          div.className = 'line';
          div.innerHTML = `<div class="name">${p.name} <small style="color:var(--muted);font-weight:600">x${qty}</small></div><div style="font-weight:700">₱${p.price*qty}</div>`;
          lines.appendChild(div);
          total += p.price * qty;
        });
        // remove one and checkout row
        const controls = document.createElement('div');
        controls.style.marginTop = '8px';
        controls.innerHTML = `<button class="btn btn-ghost" id="removeOneBtn">Remove one</button>`;
        lines.appendChild(controls);
        setTimeout(()=> {
          const removeBtn = document.getElementById('removeOneBtn');
          if(removeBtn) removeBtn.onclick = ()=> {
            const last = keys[keys.length-1];
            removeOne(last);
          };
        },50);
      }
      document.getElementById('cartTotal').textContent = `₱${total.toFixed(2)}`;
    }

    // sidebar buttons
    document.getElementById('empty').onclick = emptyCart;
    document.getElementById('checkout').onclick = ()=> {
      if(Object.keys(cart).length === 0){
        alert('Cart is empty — add some drinks first ☕');
        return;
      }
      alert('Checkout simulated — use this to gather post-order feedback in your research.');
    };

    // contact form handling (mock)
    document.getElementById('contactForm').onsubmit = function(e){
      e.preventDefault();
      const name = document.getElementById('name').value.trim();
      const purpose = document.getElementById('purpose').value;
      document.getElementById('formMsg').textContent = `Thanks ${name}! (mock) - purpose: ${purpose}`;
      this.reset();
    };
    document.getElementById('clear').onclick = ()=> { document.getElementById('contactForm').reset(); document.getElementById('formMsg').textContent = ''; };

    // image refresh (re-populate unsplash links)
    document.getElementById('imgRefreshBtn').addEventListener('click', ()=> {
      document.getElementById('hero-bg').style.backgroundImage = `url('${unsplashFor('cozy cafe interior',1400,700)}')`;
      renderProducts();
    });
    document.getElementById('imgRefreshBtn').addEventListener('keydown', (e)=> { if(e.key === 'Enter') document.getElementById('imgRefreshBtn').click(); });

    // quick scroll buttons
    document.getElementById('quick-order').onclick = ()=> document.getElementById('products').scrollIntoView({behavior:'smooth'});
    document.getElementById('howit').onclick = ()=> document.getElementById('contact').scrollIntoView({behavior:'smooth'});

    // ETA simulation
    function randomETA(){ return `${8 + Math.floor(Math.random()*8)} minutes`; }
    setInterval(()=> { document.getElementById('eta').textC